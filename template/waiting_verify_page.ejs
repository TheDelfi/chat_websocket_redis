<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Подтверждение почты</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 50px;
            text-align: center;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
            max-width: 500px;
            width: 100%;
            backdrop-filter: blur(10px);
        }

        .email-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 30px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 40px;
        }

        h1 {
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 28px;
            font-weight: 600;
        }

        p {
            color: #7f8c8d;
            line-height: 1.6;
            margin-bottom: 25px;
            font-size: 16px;
        }

        .email-address {
            color: #667eea;
            font-weight: 600;
            font-size: 18px;
            margin: 10px 0;
            word-break: break-all;
        }

        .timer {
            font-size: 48px;
            color: #667eea;
            font-weight: 700;
            margin: 30px 0;
        }

        .progress-bar {
            height: 6px;
            background: #ecf0f1;
            border-radius: 3px;
            margin: 30px 0;
            overflow: hidden;
        }

        .progress {
            height: 100%;
            background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
            width: 70%;
            border-radius: 3px;
            animation: progressAnimation 2s ease-in-out infinite;
        }

        @keyframes progressAnimation {
            0% { width: 30%; }
            50% { width: 70%; }
            100% { width: 30%; }
        }

        .resend-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
            font-size: 14px;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .resend-link:hover {
            color: #5a67d8;
            text-decoration: underline;
        }

        .support-text {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ecf0f1;
            color: #95a5a6;
            font-size: 14px;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 20px auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @media (max-width: 600px) {
            .container {
                padding: 30px 20px;
                margin: 20px;
            }
            
            h1 {
                font-size: 24px;
            }
            
            .timer {
                font-size: 36px;
            }
        }


        .resend-button {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    padding: 12px 24px;
    border-radius: 25px;
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
    display: inline-block;
    text-decoration: none;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    min-width: 160px;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.resend-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
}

.resend-button:active {
    transform: translateY(0);
    box-shadow: 0 2px 10px rgba(102, 126, 234, 0.3);
}

.resend-button:disabled {
    background: #bdc3c7;
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
}

.resend-button:disabled:hover {
    transform: none;
    box-shadow: none;
}

.resend-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
}

.resend-button:hover::before {
    left: 100%;
}

.resend-button.loading {
    position: relative;
    color: transparent;
}

.resend-button.loading::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 20px;
    height: 20px;
    margin: -10px 0 0 -10px;
    border: 2px solid transparent;
    border-top: 2px solid white;
    border-radius: 50%;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}

.resend-button.success {
    background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
}

.resend-button.error {
    background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%);
}

/* Для иконки внутри кнопки (опционально) */
.resend-button .icon {
    margin-right: 8px;
    display: inline-block;
}

/* Адаптивность */
@media (max-width: 480px) {
    .resend-button {
        padding: 10px 20px;
        font-size: 13px;
        min-width: 140px;
    }
}
    </style>
</head>
<body>
    <div class="container">
        <div class="email-icon">✉️</div>
        
        <h1>Подтвердите вашу почту</h1>
        
        <p>Мы отправили письмо с ссылкой для подтверждения на адрес:</p>
        
        <div class="email-address" id="emailPlaceholder"><%= email %></div>
        
        <p>Перейдите по ссылке в письме чтобы завершить регистрацию.</p>
        
        <div class="progress-bar">
            <div class="progress"></div>
        </div>
        
        <div class="spinner"></div>
        
        <div class="timer" id="timer">05:00</div>
        
        <p>
            Не получили письмо? 
            <form action="/auth/registration/waiting_for_verification" method="POST">
            <input type="hidden" name="email" value='<%= email %>''>
            <button onclick="resendEmail()" type="submit" class="resend-button">Отправить повторно</button>
            </form>
        </p>
        
        <div class="support-text">
            Если у вас возникли проблемы, проверьте папку "Спам" или 
            <a href="/support" style="color: #667eea;">обратитесь в поддержку</a>
        </div>
    </div>

    <script>
        // Таймер обратного отсчета
        function startTimer(duration) {
            let timer = duration;
            const timerElement = document.getElementById('timer');
            
            const interval = setInterval(() => {
                const minutes = Math.floor(timer / 60);
                const seconds = timer % 60;
                
                timerElement.textContent = `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
                
                if (--timer < 0) {
                    clearInterval(interval);
                    timerElement.textContent = '00:00';
                    document.querySelector('.resend-link').style.color = '#e74c3c';
                }
            }, 1000);
        }

        // Запуск таймера на 5 минут
        startTimer(5 * 60);

        // Функция повторной отправки
        function resendEmail() {
            alert('Письмо отправлено повторно! Проверьте вашу почту.');
            startTimer(5 * 60);
            document.querySelector('.resend-link').style.color = '#667eea';
        }

        // Подстановка email из URL или localStorage
        const urlParams = new URLSearchParams(window.location.search);
        const email = urlParams.get('email') || localStorage.getItem('registering_email');
        
        if (email) {
            document.getElementById('emailPlaceholder').textContent = email;
        }
    </script>
</body>
</html>